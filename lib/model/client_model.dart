import 'package:flutter/material.dart';

class ClientModel{
  String? pinPath;
  String? avatarPath;
  String? location;
  String? locationName;
  Color? color;

  ClientModel({this.pinPath, this.avatarPath, this.location, this.locationName,
      this.color});
}